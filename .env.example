# =====================================================
# WORLDLAND NODE COMMAND REFERENCE
# =====================================================
# Node uses command-line flags, not environment variables.
# This file documents the flags for reference.

# Example command:
# ./node \
#   -hub localhost:8443 \
#   -node-id YOUR_NODE_ID \
#   -cert node.crt \
#   -key node.key \
#   -ca ca.crt \
#   -host your-public-hostname.com \
#   -api-port 8444

# === REQUIRED FLAGS ===

# -node-id: Node ID received from Hub registration
# Get this by registering with Hub API first
# NODE_ID=your-node-id-from-registration

# -host: Public hostname/IP for SSH connections
# Renters will connect via SSH to this address
# HOST=gpu-node-1.example.com

# -cert: Path to Node certificate (PEM format)
# CERT_PATH=node.crt

# -key: Path to Node private key (PEM format)
# KEY_PATH=node.key

# -ca: Path to CA certificate (PEM format)
# CA_PATH=ca.crt

# === OPTIONAL FLAGS ===

# -hub: Hub mTLS address (default: localhost:8443)
# HUB_ADDRESS=hub.worldland.io:8443

# -api-port: Node API port (default: 8444)
# API_PORT=8444

# === CERTIFICATE GENERATION ===
# See README.md for step-ca certificate generation instructions
